package org.example.model.enums;

public enum TokenType {
    PROCEDURE,
    IF,
    THEN,
    ELSE,
    CALL,
    NAME,
    WHILE,
    INTEGER,
    PLUS,
    MINUS,
    TIMES,
    EQUAL,
    SEMICOLON,
    LBRACE,
    RBRACE,
    LPAREN,
    RPAREN,
    EOF
}
